#include "main.h"

/**
 * positive_or_negative - check s for positie or negatie
 *
 * @i- varaible to be checked
 *
 * return: alwasy 0
 */
void positive_or_negative(int i)
	if (i < 0)
		printf("%d id negative", i);
	else
		printf("%d is positive", i);
	return (0);
