#include <stdio.h>
/**
 * main - this is the main fucntion
 * Return: must alwasy be xero
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
