#include <stdio.h>
/**
 * main - this is the main fucntion
 * Return: must alwasy be xero
 */
int main(void)
{
	char i;

        for (i = 'z' ; i <= 'a' ; i--)
		putchar(i);
	putchar('\n');
	return (0);
}
